# Load Test Environment Configuration
# Copy this to .env.loadtest and modify as needed

# Base URL for the Juice Shop application
BASE_URL=http://localhost:3000

# Test scenario to run (smoke, load, stress, spike)
SCENARIO=smoke

# Type of test to run (register, login, auth-flow)
TEST_TYPE=auth-flow

# Environment identifier for tagging results
ENVIRONMENT=local

# Save detailed test results to JSON file (true/false)
SAVE_RESULTS=false

# Enable verbose HTTP debugging (true/false)
HTTP_DEBUG=false

# Grafana Cloud Configuration (for cloud visualization)
# Get your token from: https://app.k6.io/account/api-tokens
K6_CLOUD_TOKEN=your_grafana_cloud_token_here

# Optional: Project ID for organizing tests
K6_CLOUD_PROJECT_ID=12345

# Optional: Version tagging for test runs
VERSION=1.0.0

# Performance threshold overrides
# MAX_RESPONSE_TIME=1000
# MAX_ERROR_RATE=0.1
# MIN_REQUEST_RATE=10